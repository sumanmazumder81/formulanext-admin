import { DotsDirective } from './dots.directive';

describe('DotsDirective', () => {
  it('should create an instance', () => {
    const directive = new DotsDirective();
    expect(directive).toBeTruthy();
  });
});
